apiVersion: v1
kind: Service
metadata:
  name: {{ include "pm-ai-tool.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "pm-ai-tool.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
